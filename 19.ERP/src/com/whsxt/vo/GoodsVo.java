package com.whsxt.vo;

import com.whsxt.pojo.Goods;
import com.whsxt.pojo.Provider;

public class GoodsVo extends Goods {

	/**
	 * private String keywordS;
	 * 
	 * public String getKeywordS() { return keywordS; }
	 * 
	 * public void setKeywordS(String keywordS) {
	 * this.setPrice(Double.parseDouble(keywordS));
	 * this.setNumber(Integer.parseInt(keywordS));
	 * 
	 * Provider provider = new Provider(Integer.parseInt(keywordS));
	 * this.setProvider(provider);
	 * 
	 * this.keywordS = keywordS; }
	 **/
}
